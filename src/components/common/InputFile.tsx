import { MutableRefObject } from "react";

interface InputFileProps {
  directory: string;
  webkitdirector: string;
  type: string;
  ref: MutableRefObject<null | string>;
  onChange: {};
}

const InputFile = (props: InputFileProps) => {
  return <></>;
};

export default InputFile;
